# Linux Vision Deployment

Target role: Python/CUDA vision workers on Ubuntu/Linux GPU servers. The worker package must load approved model weights from local controlled storage and must not download models at runtime.
