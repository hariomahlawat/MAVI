# Offline Release Bundle

The eventual release bundle will contain every application artifact, runtime dependency, approved model weight and integrity manifest needed for an air-gapped installation. No implementation is added until dependency inventory and deployment topology are mature enough to test.
