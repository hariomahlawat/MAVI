# Database

PostgreSQL + pgvector is the approved initial authoritative datastore. Database integration is intentionally deferred until the first visual-memory data model is specified. Do not add an ORM package or schema speculatively in the repository bootstrap.
